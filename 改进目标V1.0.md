## 🧬 Agent Factory 完全体方案 — 融合 12 个外部仓库的三层架构

---

### 一、三层架构与外部仓库的映射关系

我按你定义的三层模型，将每个仓库精确分配到对应层级：

```
┌─────────────────────────────────────────────────────┐
│ Kernel Layer（内核层）     prompt_pack = system/tool/memory/output │
├─────────────────────────────────────────────────────┤
│  prompt-ez                    → system/tool 结构化   │
│  structured-prompt-builder    → system/tool/memory 分层渲染 │
│  prompt-builder-web           → system(output)/tool 可视化模板 │
├─────────────────────────────────────────────────────┤
│ Middle Layer（中间层）agent-meta.json = Agent Spec    │
├─────────────────────────────────────────────────────┤
│  agent-spec                   → 元数据标准格式         │
│  awesome-openclaw-skills      → skills 结构化定义     │
│  agent-toolkit                → skills/tools 通用封装 │
├─────────────────────────────────────────────────────┤
│ Adapter Layer（适配层）adapters/{platform}/           │
├─────────────────────────────────────────────────────┤
│  agent-package-manager        → 多平台一键打包         │
│  coze-dify-adapter            → Coze ↔ Dify 双向转换  │
│  openclaw-feishu-agent-pack   → OpenClaw → 飞书适配   │
│  feishu-power-skill           → 飞书能力注入           │
│  web-search-skill             → 通用搜索技能           │
└─────────────────────────────────────────────────────┘
```

---

### 二、每个仓库的具体提取方案

| # | 仓库 | 提取内容 | 融入位置 | 优先级 |
|---|-----|---------|---------|--------|
| 1 | **prompt-ez** | XML 结构化的 `prompt_pack` 格式（`<system>`, `<tool>`, `<memory>`, `<output>` 标签闭合） | `architect.md` 的输出约束段 → `prompt_pack` schema | 🔴 P0 |
| 2 | **structured-prompt-builder** | 三层冻结数据类：`SystemPrompt` / `ToolPrompt` / `MemoryPrompt`，保证提示词一致性校验 | `builder.py` 新增 `_build_prompt_pack()` 方法 | 🔴 P0 |
| 3 | **prompt-builder-web** | 5 类任务模板：生成类 / 审查类 / 分析类 / 对话类 / 操作类 | `architect.md` 场景引导矩阵扩展 | 🟡 P1 |
| 4 | **agent-spec** | `agent-meta.json` 标准 Schema：name, description, version, platforms, skills, memory_config, prompt_pack（完整可机器消费的元数据标准） | `builder.py` 新增 `_build_agent_meta()` 输出元数据 JSON | 🔴 P0 |
| 5 | **agent-package-manager** | 一键打包多平台命令 + ZIP 目录树规范（`prompts/` + `adapters/` + `agent-meta.json`） | `packager.py` 重组 ZIP 结构 | 🔴 P0 |
| 6 | **coze-dify-adapter** | Coze ↔ Dify 双向转换 DSL 映射表（coze.mode → dify.graph.nodes） | `builder.py` 新增 `_convert_coze_to_dify()` 和 `_convert_dify_to_coze()` | 🟡 P1 |
| 7 | **openclaw-feishu-agent-pack** | OpenClaw 的 `config.yaml` → 飞书 Bot 配置的映射（飞书卡片消息模板注入） | `builder.py` 现有的 `_build_openclaw_config` 和 `_build_feishu_bot` 增强 | 🟡 P1 |
| 8 | **awesome-openclaw-skills** | 标准化 skill 定义格式：`{name, type, api, params, output}` | `architect.md` 的 `skills` 输出字段升级（从扁平数组到结构化对象） | 🟡 P1 |
| 9 | **feishu-power-skill** | 飞书多维表格/文档/消息的 API 调用封装 → 转为 `tool` 提示词块 | `prompts/tool.md` 模板 → `prompt_pack.tool` 字段 | 🟢 P2 |
| 10 | **web-search-skill** | 通用联网搜索的提示词约束（查询重写→检索→排序→引用来源） | `prompts/tool.md` 模板增强 | 🟢 P2 |
| 11 | **agent-toolkit** | 多平台工具的标准接口抽象（base_url, auth, params, retry） | `builder.py` 新增工具配置注入到 `app.py` 模板 | 🟢 P2 |
| 12 | **Dify/Coze/OpenClaw 的 AP-ID 格式** | 各平台的变量引用语法（`{{#step_id.text}}` / `{{input}}` / `${VAR}`） | `builder.py` 的 Dify/Coze 渲染方法 | 🟡 P1 |

> *注：仓库 1-3 的 `node_modules` 产物（web 入口、npm 包）不需要提取，只需提取它们的核心概念——结构化提示词格式和模板分类。*

---

### 三、调整后的生产计划（融合版，9 项）

```
Phase 1 — 内核层升级（architect.md）
  ✅ 1️⃣ 升级 architect.md — prompt_pack 拆分输出
     ├─ 新增 output 字段：description / version / platforms / skills(structured) / memory_config / prompt_pack
     ├─ prompt_pack 拆分为 4 段：<system> / <tool> / <memory> / <output>
     ├─ 融合 structured-prompt-builder 的三层冻结数据类思想（保证一致性）
     └─ 融合 prompt-ez 的 XML 闭合标签结构

Phase 2 — 中间层升级（builder.py）
  ✅ 2️⃣ 升级 builder.py — agent-meta.json 生成
     ├─ 新增 _build_agent_meta() → 输出符合 agent-spec 标准的 agent-meta.json
     ├─ 新增 _build_prompt_pack() → 将 system_prompt 拆分写入 prompts/ 目录
     └─ 重构 _build_platform_files() → adapters/ 子路径

Phase 3 — 适配层升级（builder.py 增强）
  ✅ 3️⃣ 增强 builder.py — Coze ↔ Dify 双向转换
     ├─ 新增 _convert_coze_to_dify() / _convert_dify_to_coze()
     ├─ 从 coze-dify-adapter 提取映射表
     └─ 从 openclaw-feishu-agent-pack 提取飞书卡片消息模板

  ✅ 4️⃣ 增强 builder.py — skills/tools 标准化
     ├─ skills 从扁平数组升级为结构化对象（融合 awesome-openclaw-skills 格式）
     └─ tool 配置注入到 app.py 模板（融合 agent-toolkit 接口抽象）
     
  ✅ 5️⃣ 增强 builder.py — AP-ID 变量语法统一
     └─ 各平台引用语法精确映射（已部分完成，需验证）

Phase 4 — 打包器升级（packager.py）
  ✅ 6️⃣ 升级 packager.py — ZIP 目录树重组
     ├─ 新结构：prompts/ + adapters/coze/ + adapters/dify/ + adapters/openclaw/ + adapters/feishu/
     ├─ 新增 import.sh 一键导入脚本（融合 agent-package-manager 打包命令）
     └─ 新增 _generate_import_sh() → 自动检测平台并导入

Phase 5 — 控制台升级（factory_app.py）
  ✅ 7️⃣ 升级 factory_app.py — 新增记忆配置与多平台选择器

Phase 6 — 工作流模板库充实
  ✅ 8️⃣ 从 7 个顶流开源项目提取真实工作流
     ├─ CrewAI / LangGraph / OpenDevin / BabyAGI / Reflexion / SWE-Agent / Camel
     └─ 去重保留 10-12 个最具差异化的模板

Phase 7 — 验证
  ✅ 9️⃣ 端到端验证：llm_client.py 适配 → Docker 重建 → 完整流水线测试
```

---

### 四、agent-meta.json 目标 Schema（融合 agent-spec 标准）

```json
{
  "name": "挑战杯路演PPT润色助手-李言",
  "description": "专业路演教练，帮你在 5 分钟内打造评委无法拒绝的 PPT",
  "version": "1.0.0",
  "platforms": ["coze", "dify", "openclaw", "feishu"],
  "auth_mode": "user_key",
  "skills": [
    { "name": "商业叙事架构", "type": "analysis", "description": "构建投资人视角的故事线" },
    { "name": "视觉层级设计", "type": "design", "description": "优化信息密度与视觉焦点" }
  ],
  "memory_config": {
    "type": "short_term",
    "max_turns": 5,
    "persist_strategy": "session_only"
  },
  "prompt_pack": {
    "system": "我是李言，一位拥有8年经验的路演教练...",
    "tool": "你可以调用以下工具：search(查询最新行业数据)...",
    "memory": "维护一个内部记忆缓存，记录用户的演讲风格偏好...",
    "output": "输出两份内容：1) Markdown 改写稿 2) JSON 结语节点..."
  },
  "workflow_steps": [
    { "step_id": "step_1", "name": "意图识别", "prompt": "...", "gate": "置信度≥0.8" },
    { "step_id": "step_2", "name": "内容分析", "prompt": "...", "gate": "覆盖率≥90%" },
    { "step_id": "step_3", "name": "润色重构", "prompt": "...", "gate": "改进点数≥3" },
    { "step_id": "step_4", "name": "质量审核", "prompt": "...", "gate": "自检通过" }
  ]
}
```

---

### 五、ZIP 打包目标目录树（融合 agent-package-manager 规范）

```
挑战杯路演PPT润色助手_v1.0/
├── agent-meta.json                     ← 元数据标准（任何平台第一入口）
├── prompts/                            ← 内核层
│   ├── system.md
│   ├── tool.md
│   ├── memory.md
│   └── output.md
├── adapters/                           ← 适配层（平台独立子目录）
│   ├── coze/
│   │   └── bot.json
│   ├── dify/
│   │   └── agent.yaml
│   ├── openclaw/
│   │   └── config.yaml
│   └── feishu/
│       ├── bot-config.yaml
│       └── bot-server.py
├── app.py                              ← CLI 版
├── app_web.py                          ← Web 版
├── requirements.txt
├── 配置_API_Key.env
├── 一键启动_Windows.bat
├── 一键启动_Mac.sh
├── import.sh                           ← 一键导入（自动检测平台）
└── 使用说明书.docx
```

---

### 六、改动量评估

| Phase | 涉及文件 | 新增行 | 修改行 | 耗时 |
|-------|---------|--------|--------|------|
| Phase 1 | architect.md | ~120 | ~30 | 2h |
| Phase 2 | builder.py | ~150 | ~40 | 2.5h |
| Phase 3 | builder.py | ~200 | ~50 | 3h |
| Phase 4 | packager.py | ~100 | ~60 | 2h |
| Phase 5 | factory_app.py | ~30 | ~10 | 0.5h |
| Phase 6 | 7 个 workflow JSON | ~350 | 0 | 2h |
| Phase 7 | llm_client.py + Docker | ~20 | ~10 | 1h |
| **总计** | 7 个文件 | **~970 行** | **~200 行** | **~13h** |

---
